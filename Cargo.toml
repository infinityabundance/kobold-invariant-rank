[package]
name = "kobold-invariant-rank"
version = "0.1.0"
edition = "2021"
description = "Real-ecosystem x generated-oracle-witness ranking: feature-cell vocabulary, public-occurrence/oracle-sharpness/migration-risk scoring, and court-candidate prioritization."
authors = ["infinityabundance"]
license = "Apache-2.0"
repository = "https://github.com/infinityabundance/kobold-invariant-rank"
readme = "README.md"
publish = false  # empty scaffold; flip to publishable once the implementation lands

[dependencies]
# Dependency rule: kobold-* MAY depend on gnucobol-rs; gnucobol-rs MUST NOT depend on kobold-*.
# Activate when the implementation is extracted, e.g.: gnucobol-rs = "0.7"
